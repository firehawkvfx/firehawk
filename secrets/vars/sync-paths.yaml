sync_paths:
  - path: "{{ show }}/{{ seq }}/{{ shot }}/houdini"
    s3_push_args: --include "*.hip" --exclude "*backup/*" --exclude "*geo/*" --exclude "*pdgtemp/*"
  - path: "{{ show }}/assets"
  - path: "/asset"
  - path: "{{ show }}/{{ seq }}/{{ shot }}/assets"
  # - path: "{{ show }}/{{ seq }}/{{ shot }}/cache"

remote_push_paths:
  - path: "{{ show }}/{{ seq }}/{{ shot }}/render"
  - path: "{{ show }}/{{ seq }}/{{ shot }}/cache"
  - path: "{{ show }}/{{ seq }}/{{ shot }}/houdini"
    s3_push_args: --include "*.hip" --exclude "*backup/*" --exclude "*geo/*" --exclude "*pdgtemp/*"
  - path: "{{ show }}/assets"
  - path: "/asset"
  - path: "{{ show }}/{{ seq }}/{{ shot }}/assets"
  