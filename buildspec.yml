version: 0.2

phases:
  install:
    commands:
      - git submodule init
      - git submodule update --recursive
      - ./install-packages
  build:
    commands:
      - echo '...replace with real build commands...'

artifacts:
  files:
    - '**/*'